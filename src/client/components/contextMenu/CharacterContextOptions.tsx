import React from "react";

type contextProp = {
  contextId: string;
};
export default function CharacterContextActions(props: contextProp) {
  // TODO: Get character info from the database using contextId and populate the context options, (send money, mail, etc.)
  return <div>Player Context Options</div>;
}
